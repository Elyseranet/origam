/**
 * Vertical alignment of items inside a single `<OrigamMasonry>`
 * column. See `TMasonryAlign` for the per-value rationale (only the
 * JS fallback path honours `CENTER` — the CSS-native path always
 * aligns to `TOP`).
 */
export enum MASONRY_ALIGN {
    TOP = 'top',
    CENTER = 'center'
}
