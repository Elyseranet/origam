/**
 * Stacking direction for `<OrigamSnackbarGroup>`. See
 * `TSnackbarGroupDirection` for the per-value rationale.
 */
export enum SNACKBAR_GROUP_DIRECTION {
    TOP_DOWN = 'top-down',
    BOTTOM_UP = 'bottom-up'
}

/**
 * Anchor locations supported by `<OrigamSnackbarGroup>`. See
 * `TSnackbarGroupLocation` for the per-value rationale.
 */
export enum SNACKBAR_GROUP_LOCATION {
    TOP_LEFT = 'top-left',
    TOP_RIGHT = 'top-right',
    TOP_CENTER = 'top-center',
    BOTTOM_LEFT = 'bottom-left',
    BOTTOM_RIGHT = 'bottom-right',
    BOTTOM_CENTER = 'bottom-center',
    TOP = 'top',
    BOTTOM = 'bottom'
}
