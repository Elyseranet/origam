/**
 * How a multi-file selection is rendered by `<OrigamFileField>`. See
 * `TFileFieldDisplay` for the per-value rationale.
 */
export enum FILE_FIELD_DISPLAY {
    LIST = 'list',
    CHIPS = 'chips',
    COUNTER = 'counter'
}
