/**
 * Font-family token keys. Map to `--origam-font__family---{key}`:
 * sans (Inter / Helvetica Neue / Arial) · mono (JetBrains Mono / Fira Code) ·
 * serif (Georgia / Times New Roman)
 */
export enum FONT_FAMILY {
    SANS = 'sans',
    MONO = 'mono',
    SERIF = 'serif'
}
