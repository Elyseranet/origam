/**
 * Animation strategy applied to the background painted under the
 * `background-clip: text` mask of `<OrigamTextMask>`. See
 * `TTextMaskAnimation` for the per-value rationale.
 */
export enum TEXT_MASK_ANIMATION {
    PAN = 'pan',
    ROTATE = 'rotate',
    PULSE = 'pulse',
    ZOOM = 'zoom'
}
