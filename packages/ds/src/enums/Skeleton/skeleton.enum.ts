/**
 * Shape presets for `<OrigamSkeleton>`. Each value drives a different
 * placeholder geometry (line runs, aspect ratio, composed sub-blocks).
 */
export enum SKELETON_VARIANT {
    TEXT = 'text',
    RECTANGULAR = 'rectangular',
    CIRCULAR = 'circular',
    CARD = 'card',
    LIST_ITEM = 'list-item'
}
