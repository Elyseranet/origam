/**
 * Visual variants for `<OrigamKbd>`. Narrower than the Commons
 * `VARIANT` taxonomy — the keycap surface only supports these three
 * treatments (see the `--variant-{value}` SCSS blocks).
 */
export enum KBD_VARIANT {
    FILLED = 'filled',
    OUTLINED = 'outlined',
    TONAL = 'tonal'
}
