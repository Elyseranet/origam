export enum BLOCKQUOTE_LANG {
    FR = 'fr',
    EN = 'en',
    ES = 'es',
    DE = 'de',
    AUTO = 'auto'
}

export enum BLOCKQUOTE_VARIANT {
    DEFAULT = 'default',
    ELEGANT = 'elegant',
    QUOTED = 'quoted',
    MINIMAL = 'minimal',
    PULL = 'pull'
}
