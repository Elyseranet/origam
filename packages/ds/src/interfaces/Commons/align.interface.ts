import type { TAlign } from '../../types/Commons/align.type'

export interface IAlignProps {
    align?: TAlign,
    alignSm?: TAlign,
    alignMd?: TAlign,
    alignLg?: TAlign,
    alignXl?: TAlign,
    alignXxl?: TAlign
}
