import type { TJustify } from '../../types/Commons/justify.type'

export interface IJustifyProps {
    justify?: TJustify,
    justifySm?: TJustify,
    justifyMd?: TJustify,
    justifyLg?: TJustify,
    justifyXl?: TJustify,
    justifyXxl?: TJustify
}
