{
  "_comment": "OrigamChartStreamgraph component tokens — issue #503 (Paquet 1, cause C). Values copied verbatim from OrigamChartStreamgraph.vue's SCSS fallbacks. Shared chart.json vars are out of scope (tracked by #436-A).",

  "chart-streamgraph": {
    "grid":      { "stroke": { "$type": "color", "$value": "#e5e7eb", "$description": "No origam token matches #e5e7eb." } },
    "axis":      { "stroke": { "$type": "color", "$value": "#d1d5db", "$description": "No origam token matches #d1d5db." } },
    "crosshair": { "stroke": { "$type": "color", "$value": "#6b7280", "$description": "No origam token matches #6b7280." } },
    "ribbon":    { "opacity": { "$type": "number", "$value": 0.82, "$description": "No opacity.* primitive matches 0.82 (closest is opacity.87) — kept literal." } },

    "$child": {
      "axis-label": {
        "font-size": { "$type": "dimension", "$value": "0.6875rem" },
        "fill":      { "$type": "color",     "$value": "#6b7280", "$description": "No origam token matches #6b7280." }
      }
    }
  }
}
