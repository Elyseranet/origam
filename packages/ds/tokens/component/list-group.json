{
  "_comment": "OrigamListGroup component tokens — issue #503 (Paquet 1, cause C). The component's own CSS class (.origam-list-group) is a standalone compound block, NOT a BEM child of list (contrast with list.json's existing 'group' key, nested under 'list', which emits the mismatched --origam-list__group---* — never read by this component). Values copied verbatim from OrigamListGroup.vue's SCSS fallbacks. NOTE: --origam-list-group---list-indent-size and --origam-list-group---parent-padding (lines 216/219/224/228) are locally synthesised (declared AND read within the same component, recursive indent plumbing) — not part of the guard's 'dead' set and correctly left untokenised here.",

  "list-group": {
    "prepend-width": { "$type": "dimension", "$value": "{space.4}", "$description": "Literal fallback 16px matches space.4 exactly." },

    "$child": {
      "header": {
        "$state": {
          "active": {
            "opacity": { "$type": "number", "$value": "{opacity.0}" },

            "$state": {
              "hover": {
                "opacity": { "$type": "number", "$value": "calc(0.04 * 1)", "$description": "Compound calc() expression — no opacity.* primitive applies as-is." }
              }
            }
          }
        }
      }
    },

    "$state": {
      "fluid": {
        "_comment": "Forced under $state — 'fluid' is NOT a member of the built-in INTENT_STATES set (token-name.mjs), so the unmarked heuristic would otherwise misclassify it as a BEM child (--list-group__fluid---*) instead of the correct state form (--list-group--fluid---*).",
        "list-indent-size": { "$type": "dimension", "$value": "{space.0}", "$description": "Literal fallback 0px matches space.0 exactly." }
      }
    }
  }
}
