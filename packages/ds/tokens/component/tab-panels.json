{
  "_comment": "OrigamTabPanel / OrigamTabPanels component tokens — issue #503 (Paquet 1, cause C). Both components read the --origam-tab-panels__panel---* prefix, a standalone compound block distinct from tabs.json's existing 'panel' key (nested under 'tabs', which emits the mismatched --origam-tabs__panel---* — a different root block word entirely, never read by either component). Values copied verbatim from OrigamTabPanel.vue's SCSS fallbacks. NOTE: --origam-tab-panels__panel---padding-block/padding-inline (read by OrigamTabPanels.vue) are locally short-circuited via a global, non-scoped `<style>:root{}` block written directly in that component (same literal values, 16px/0) — not part of the guard's 'dead' set and out of this file's scope; flagged separately, see delivery report.",

  "tab-panels": {
    "panel": {
      "transition-duration": { "$type": "duration", "$value": "0.18s", "$description": "No motion.duration.* primitive matches 180ms." },

      "focus": {
        "outline-color": { "$type": "color", "$value": "currentColor" }
      }
    }
  }
}
