{
  "_comment": "OrigamChartRangeSelector component tokens — issue #503 (Paquet 1, cause C). Values copied verbatim from OrigamChartRangeSelector.vue's SCSS fallbacks. This component owns its own root/list/btn vars entirely — it shares nothing with chart.json.",

  "chart-range-selector": {
    "padding-inline": { "$type": "dimension", "$value": "{space.1}", "$description": "Literal fallback 4px matches space.1 exactly." },

    "list": { "gap": { "$type": "dimension", "$value": "2px", "$description": "No space.* primitive matches 2px." } },

    "btn": {
      "padding":        { "$type": "dimension", "$value": "3px 8px", "$description": "Compound shorthand — no single space.* token applies." },
      "border-color":   { "$type": "color",     "$value": "#d1d5db", "$description": "Legacy stale ref var(--origam-color-border-default, #d1d5db) — no origam token matches #d1d5db." },
      "border-radius":  { "$type": "dimension", "$value": "{radius.sm}", "$description": "Literal fallback 4px matches radius.sm exactly." },
      "background-color": { "$type": "color",  "$value": "transparent" },
      "color":          { "$type": "color",     "$value": "#6b7280", "$description": "Legacy stale ref var(--origam-color-text-secondary, #6b7280) — no origam token matches #6b7280." },
      "font-size":      { "$type": "dimension", "$value": "{font.size.sm}", "$description": "Literal fallback 0.75rem matches font.size.sm exactly." },

      "hover": {
        "background-color": { "$type": "color", "$value": "rgba(0, 0, 0, 0.06)" },
        "color":             { "$type": "color", "$value": "#111827", "$description": "Legacy stale ref var(--origam-color-text-primary, #111827) — no origam token matches #111827." }
      },
      "active": {
        "background-color": { "$type": "color", "$value": "#3b82f6", "$description": "Legacy stale ref var(--origam-color-action-primary-bg-default, #3b82f6) — no origam token matches #3b82f6 (origam's primary ramp is a different hue entirely)." },
        "border-color":      { "$type": "color", "$value": "transparent" },
        "color":             { "$type": "color", "$value": "{color.white}", "$description": "Literal fallback #ffffff matches color.white exactly." }
      }
    },

    "$child": {
      "btn": {
        "$state": {
          "active": {
            "$state": {
              "hover": {
                "background-color": { "$type": "color", "$value": "#2563eb", "$description": "Legacy stale ref var(--origam-color-action-primary-bg-hover, #2563eb) — no origam token matches #2563eb. Chained state (btn--active--hover) requires explicit $child/$state markers — the legacy heuristic only resolves one state level after a bare child." },
                "color":             { "$type": "color", "$value": "{color.white}", "$description": "Literal fallback #ffffff matches color.white exactly." }
              }
            }
          }
        }
      }
    }
  }
}
