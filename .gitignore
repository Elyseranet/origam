# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node modules
node_modules

# Cache de compilation Node ecrit par le postinstall de marketing
# (`nuxt prepare`). Genere par CHAQUE `pnpm install`, jamais versionne, et
# jusqu'ici non ignore : 1448 fichiers non suivis dans un worktree frais,
# soit autant de candidats a un `git add -A` distrait. Mesure le 2026-09-16.
packages/marketing/node-compile-cache/

# Sonde memoire ecrite A LA VOLEE par packages/tests/bench/marketing-ssr-leak.mjs
# (#248) et supprimee en sortie. Ignoree ici comme filet : un `kill -9` du bench
# la laisserait sur place, et c'est une route de diagnostic qui n'a rien a faire
# dans l'arbre ni, a plus forte raison, en production.
packages/marketing/server/api/__mem.get.ts

# Dist
dist
dist-ssr

# Vitepress
docs/.vitepress/dist
docs/.vitepress/cache
packages/docs/.vitepress/dist
packages/docs/.vitepress/cache

# Local files
*.local

# Editor directories and files
.vscode
!.vscode/extensions.json
.idea
.claude
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test
tests/e2e/.report
tests/e2e/.results
tests/a11y/.report
tests/a11y/.results
packages/tests/e2e/.report
packages/tests/e2e/.results
packages/tests/e2e/.report-marketing
packages/tests/e2e/.results-marketing
packages/tests/a11y/.report
packages/tests/a11y/.results
packages/tests/a11y/.report-marketing
packages/tests/a11y/.results-marketing
packages/tests/vrt/.report
packages/tests/vrt/.results

# VRT baselines are ONLY trustworthy when captured in the pinned Linux
# container documented in packages/tests/vrt/VRT.md. Playwright's default
# snapshot naming embeds {platform}, so a baseline captured natively on a
# contributor's machine lands in a *-darwin.png / *-win32.png file distinct
# from the committed *-linux.png ones. Ignore those so an accidental native
# `--update-snapshots` run can never be committed by mistake.
packages/tests/vrt/**/*-darwin.png
packages/tests/vrt/**/*-win32.png

# Debug files
*.debug*


**/*/maquettes/
**/*/wireframes/

# TypeScript / tsc build artefacts — never track
# These are emitted by `tsc --emitDeclarationOnly` or by the IDE's
# Volar TS plugin. The repo authors only `.ts` / `.vue` / `.scss` in
# `src/`; any sibling `.js` / `.d.ts` / `.vue.js` is a build artefact.
*.tsbuildinfo
*.vue.js
src/**/*.js
src/components/**/*.d.ts
src/composables/**/*.d.ts
src/consts/**/*.d.ts
src/directives/**/*.d.ts
src/enums/**/*.d.ts
src/interfaces/**/*.d.ts
src/layouts/**/*.d.ts
src/types/**/*.d.ts
src/utils/**/*.d.ts
src/services/**/*.d.ts
src/stores/**/*.d.ts
src/assets/**/*.d.ts
src/factories/**/*.d.ts
src/icons/**/*.d.ts
src/locales/**/*.d.ts
src/plugins/**/*.d.ts
vite.config.js
vite.config.d.ts
docs/tsconfig.tsbuildinfo
origam-*.tgz

# Multi-PM support — only pnpm-lock.yaml is committed as the source of
# truth. Contributors can install via npm or yarn, but their generated
# lockfiles stay local to avoid the 3-way lockfile-drift problem.
package-lock.json
yarn.lock
.yarn/
.pnp.*

# Test coverage output (generated; consumed by SonarQube in CI)
coverage/
# design-sync (artefacts régénérés — la config/notes/scripts dans .design-sync/ SONT committés)
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Copie generee au packaging (prepack) — la source est le CHANGELOG.md racine
packages/ds/CHANGELOG.md

# Secrets locaux — ne JAMAIS versionner (le depot est public)
.env.local
.env.*.local
*.token

# Fichiers d environnement — jamais suivis (cf. politique globale)
.env
